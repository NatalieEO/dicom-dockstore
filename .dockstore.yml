version: 1.1
service:
  type: docker-compose
  name: DICOM viewer
  description: |
    DICOM Viewer
  files:
     - docker-compose.yml
     - README.md
     - config/nginx-with-proxy.conf
     - config/orthanc.json

  scripts:
    start: "docker-compose up -d"
    port: "echo 3000"
    stop: "docker-compose down"

  environment:
    DATA_DIR:
        default: "./data"
        description: The location of the data

